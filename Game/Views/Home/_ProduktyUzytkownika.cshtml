@model List<Game.Models.UserProducts>
<div class=".col-md-8">
    <table class="table table-hover">
        <tr>
            @foreach (var b in Model)
            {

                <td>@b.Product : @b.Value</td>

            }
        </tr>
    </table>
</div>
