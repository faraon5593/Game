@model Game.Models.Maps

<table class="mapa">
    @for (int i = 0; i < Model.Height; i++)
    {
        <tr>
            @for (int j = 0; j < Model.Width; j++)
            {
                <td class="mapa_trawa"></td>
            }
        </tr>
    }
</table>